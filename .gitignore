# dependencies
/node_modules
/.pnp
.pnp.js
.pnpm-store

# next.js
/.next/
/out/
next-env.d.ts
next.config.ts.bak

# build artifacts
/build
/dist
*.tsbuildinfo

# prisma
/prisma/migrations/dev.db*
/prisma/*.db
/prisma/*.db-journal
/prisma/.tmp

# generated client (regenerated by `postinstall: prisma generate`)
/generated

# env files (NEVER commit secrets)
/.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# keep .env.example tracked — it has no secrets
!.env.example

# logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# testing / coverage
/coverage
/.nyc_output

# misc OS / editor junk
.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# editors
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
*.swp
*.swo
*.swn
*.bak
*.tmp
*.orig

# vercel / netlify
.vercel
.netlify

# typescript
*.tsbuildinfo

# certs / keys — never commit
*.pem
*.key
*.crt
*.p12

# turbo
.turbo

# claude / cursor / aider local state
.claude/settings.local.json
.cursor/
.aider*

# serena (local MCP cache)
.serena/

# package manager
yarn.lock
package-lock.json


.agent-teams
design/

# ─── 数据资源(均不上传,本地保留) ────────────────
# 电子表格 / 数据集
*.xlsx
*.xls
*.csv
*.zip
*.7z
*.db

# 数据源文件(scripts 一次导入用,数据已入库)
scripts/region.json
scripts/community-address.txt

# ─── Python / uv (src/ner 地址 NER 工程) ────────────
.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/

# 预训练模型(393MB,不入库;换机需从 HuggingFace 重新下载)
src/ner/base/

# 训练产物 / 数据产物(不入库)
src/ner/model/
src/ner/data/

# 标注数据(Label Studio 导出,数据资源不上传)
src/ner/label/exported/
